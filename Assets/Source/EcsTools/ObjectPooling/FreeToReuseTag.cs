namespace EcsTools.ObjectPooling
{
	/// <summary>
	/// Use this tag to mark entities that can be reused.
	/// Entities with this tag can be treated as free(ready to reuse) in object pool.
	/// </summary>
	public struct FreeToReuseTag
	{
		
	}
}